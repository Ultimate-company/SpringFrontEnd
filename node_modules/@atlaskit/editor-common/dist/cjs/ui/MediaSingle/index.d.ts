import React from 'react';
import { MediaSingleLayout } from '@atlaskit/adf-schema';
export declare const DEFAULT_IMAGE_WIDTH = 250;
export declare const DEFAULT_IMAGE_HEIGHT = 200;
export interface Props {
    children: React.ReactChild;
    layout: MediaSingleLayout;
    width: number;
    height: number;
    lineLength: number;
    containerWidth?: number;
    isLoading?: boolean;
    className?: string;
    pctWidth?: number;
    fullWidthMode?: boolean;
}
export default function MediaSingle({ children, layout, width, height, containerWidth, isLoading, pctWidth, className, fullWidthMode, lineLength, }: Props): JSX.Element;
